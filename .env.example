# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Storage bucket name (defaults to 'course-images')
# VITE_IMAGES_BUCKET=course-images

# Dintero Configuration
# No publishable key on the client — Dintero's Web SDK loads from their CDN using session IDs only.
#
# Server-side Edge Function secrets (set in Supabase, not in this file):
#   supabase secrets set DINTERO_ACCOUNT_ID=T12345678      # T-prefix for sandbox, P-prefix for prod
#   supabase secrets set DINTERO_CLIENT_ID=...             # From Dintero Backoffice → Settings → API
#   supabase secrets set DINTERO_CLIENT_SECRET=...
#   supabase secrets set DINTERO_WEBHOOK_SECRET=...        # Create via POST /v1/admin/signature
#   supabase secrets set DINTERO_PROFILE_ID=default        # Optional, defaults to 'default'
#   supabase secrets set DINTERO_CRON_SECRET=...           # Shared secret for sync-dintero-seller-statuses

# Optional: Feature flags
# VITE_ENABLE_REALTIME=false
# VITE_ENABLE_ANALYTICS=false

# Optional: Pagination defaults
# VITE_DEFAULT_PAGE_SIZE=20
